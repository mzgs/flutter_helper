name: mzgs_flutter_helper
description: Flutter helper library for ui, file download , list files, in app purchase and subscription
version: 0.0.122
homepage: https://github.com/mzgs/flutter_helper

# dart pub publish -f

# mzgs_flutter_helper:
#        path: /Users/mustafa/Developer/Flutter/flutter_helper

environment:
  sdk: '>=3.3.3 <4.0.0'
  flutter: ">=1.19.5"

dependencies:     
  flutter:
    sdk: flutter
  dio: ^5.1.1
  intl: ^0.18.1
  url_launcher: ^6.1.10
  path_provider:
  in_app_review: ^2.0.8
  webview_flutter: ^4.4.2
  get: ^4.6.5
  get_storage: ^2.1.1
  flutter_asa_attribution: ^0.0.3
  gdpr_dialog: ^2.1.1
  http:  
  package_info_plus: 
  rating_dialog: ^2.0.4
  permission_handler: ^11.0.0
  applovin_max: ^3.7.0
  html: ^0.15.4
  google_mobile_ads: ^4.0.0
  share_plus: ^7.2.1
  event_bus: ^2.0.0
  device_info_plus:
  storekit2helper: ^0.0.11
  

 
 
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter: